name: netflix_clone
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_bloc: ^8.1.3
  flutter_html: ^3.0.0-beta.2
  flare_flutter: ^3.0.2
  flutter_native_splash: ^2.3.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  # flutter_lints: ^2.0.0

flutter_native_splash:
  color: "#000000"
  image: assets/icons/netflix_icon.png
  image_dark: assets/icons/netflix_icon.png
  color_dark: "#000000"
  android: true
  ios: true

flutter:
  uses-material-design: true
  assets:
    - assets/icons/